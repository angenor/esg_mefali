-r requirements.txt

# Tests
pytest>=8.3.0
pytest-asyncio>=0.25.0
httpx>=0.28.0
pytest-cov>=6.0.0
aiosqlite>=0.20.0
freezegun>=1.5.0
psycopg2-binary>=2.9.9  # driver sync requis par tests migrations Story 10.1 + scripts/validate_schema.py

# Mock AWS (Story 10.6 — marker @pytest.mark.s3)
moto[s3]>=5.0,<6.0

# HCL parsing (Story 10.7 — tests IAM policies Terraform)
python-hcl2>=4.3,<5

# Linting
ruff>=0.8.0
